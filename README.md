**Build:** ```g++ -std=c++17 main.cpp -o climg```

**Run:** ```./climg <image path> [exposure]```
- **exposure**: optional ```float``` (default = ```2.5```)

*(Adjust terminal zoom and size to change resolution.)*
